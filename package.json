{
  "name": "@nekr/scripts",
  "version": "1.1.1",
  "description": "",
  "main": "dist/index.js",
  "files": [
    "dist/",
    "src/"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src/ --out-dir ./dist --presets es2015 --plugins babel-plugin-add-module-exports --loose",
    "prepublish": "npm run build",
    "release": "npm publish --access=public"
  },
  "author": "Arthur Stolyar <nekr.fabula@gmail.com>",
  "license": "UNLICENSED",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015": "^6.18.0"
  }
}
